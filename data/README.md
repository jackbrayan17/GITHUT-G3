# Data directories

- `raw/` contains original datasets, especially files tracked with Git LFS.
- `processed/` contains derived datasets generated from notebooks or scripts.

Do not commit sensitive data without permission.
